# 把《历史文字模拟器》发布到网上

这个游戏是纯静态网页，不依赖后端。只要把文件夹里的 5 个文件放到任意静态网页托管服务上，别人就能通过网址访问。

## 方式一：Netlify Drop（最快，免费）

1. 打开 https://app.netlify.com/drop
2. 把 `历史文字模拟器` 文件夹整个拖进去
3. 等几秒钟，Netlify 会生成一个网址，例如：
   `https://你的名字.netlify.app`
4. 把网址发给别人即可

## 方式二：GitHub Pages（免费，适合长期保存）

1. 注册 GitHub，新建仓库，例如 `history-simulator`
2. 把 `index.html`、`style.css`、`data.js`、`game.js` 传到仓库根目录
3. 进入仓库 `Settings` → `Pages`
4. Source 选择 `Deploy from a branch`，Branch 选择 `main`、目录选 `/ (root)`
5. 保存后得到网址：
   `https://你的用户名.github.io/history-simulator/`

## 方式三：Vercel（免费）

1. 安装 Node.js 后执行：
   ```bash
   npm i -g vercel
   cd 历史文字模拟器
   vercel deploy
   ```
2. 按提示完成即可，会生成 `xxx.vercel.app` 网址

## 方式四：Gitee Pages（国内访问较快）

1. 注册 Gitee，新建仓库并上传这 5 个文件
2. 进入仓库 `服务` → `Gitee Pages`
3. 开启 Pages，得到国内访问网址

## 方式五：云服务器 / 对象存储（更稳定，适合长期运营）

- 阿里云 OSS、腾讯云 COS：开启静态网站托管，上传这 5 个文件即可；
- 云服务器：安装任意静态服务器，例如：
  ```bash
  cd 历史文字模拟器
  python3 -m http.server 8080
  ```
  再用 Nginx 等做域名和 HTTPS 配置。

## 注意事项

- 语音朗读使用浏览器自带的 Web Speech API，必须在 `https://` 或 `localhost` 下运行；
- 不同手机、电脑是否有人声，取决于系统是否安装了中文语音包；
- 游戏存档保存在玩家自己的浏览器 localStorage 中，不会上传到服务器。
